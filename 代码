路径是我猜的，你的不一定叫这个。先用 `find` 把文件找出来。

## 找 xacro 文件

```bash
find ~/dummy_ws -name "*.xacro"
find ~/dummy_ws -name "*.urdf"
```

第一条列出所有 xacro，里面应该有一个名字带 `dummy-ros2` 的主文件（就是你之前贴给我的那个带 `<xacro:include>` 和所有 link 的）。第二条看有没有现成的 urdf。

也可以直接定位 urdf 目录：

```bash
find ~/dummy_ws/src -type d -name "urdf"
```

找到后进去看：

```bash
ls -l ~/dummy_ws/src/dummy-ros2_description/urdf/   # 路径按 find 结果改
```

应该能看到 `materials.xacro`、`dummy-ros2.trans`、`dummy-ros2.gazebo`，以及那个主 xacro（可能叫 `dummy-ros2.xacro` 或 `dummy-ros2.urdf.xacro`）。用真实文件名替换我之前命令里的路径再跑 `xacro`。

## 更省事的办法：直接从运行中的机器人导出 URDF

其实 Gazebo 启动时，`robot_state_publisher` 里已经存着**展开好的完整 URDF**（xacro 已经处理过了）。如果你嫌找 xacro 麻烦，**保持 Gazebo 在跑**，直接导出：

```bash
ros2 param get /robot_state_publisher robot_description > /tmp/dummy-ros2_raw.txt
```

这个文件开头会有一行 `String value is:` 的前缀，需要删掉，让文件从 `<?xml` 开始、到 `</robot>` 结束。可以用文本编辑器删，或者：

```bash
# 去掉第一行前缀，得到干净 urdf
tail -n +1 /tmp/dummy-ros2_raw.txt | sed 's/^String value is: //' > /tmp/dummy-ros2.urdf
```

然后检查一下确实是合法 urdf：

```bash
head -5 /tmp/dummy-ros2.urdf     # 第一行应是 <?xml ... ?> 或 <robot ...>
tail -3 /tmp/dummy-ros2.urdf     # 最后应是 </robot>
```

这个导出版本的好处是 **xacro 全展开了、include 都合并了**，importrobot 读起来最省心。

## 提醒：mesh 路径

不管哪种方式得到的 urdf，里面 mesh 都是 `file://$(find dummy-ros2_description)/meshes/xxx.stl` 这种路径，MATLAB 的 `importrobot` 解析不了。两个办法：
- importrobot 默认会**跳过解析不了的 mesh**（只是没有可视化外形，运动学/动力学不受影响）——对我们够用了。
- 如果它报错，加参数让它忽略 mesh：`robot = importrobot('dummy-ros2.urdf', 'MeshPath', {});`

先跑 `find ~/dummy_ws -name "*.xacro"`，把输出贴上来，我帮你确认主文件名和完整命令。或者直接用上面的 `robot_state_publisher` 导出法，也行。
